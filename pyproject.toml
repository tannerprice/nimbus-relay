[project]
name = "nimbus-relay"
version = "0.1.0"
description = "RTL-SDR NOAA Weather Radio SAME relay for Nimbus HA"
requires-python = ">=3.12"
dependencies = [
  "paho-mqtt>=2.0.0",
  "python-dotenv>=1.0.1"
]

[project.scripts]
nimbus-relay = "nimbus_relay.main:main"
